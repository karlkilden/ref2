package com.kildeen.gv;

public class MethodConventions {
	/*
	 * set
	 * get - throws exception if not found
	 * find - like get but no exception
	 * switchXXx - A method call to load another entity in interfaces
	 * loadXxx - A call to load or reload a certain part of the page
	 * saveXxx - Action call to flush new values to the persistence layer. Sometimes save is enough but soon other things can be saved and xxx is needed in the name
	 * refreshXxx - A call to refresh the currently loaded and cached information
	 * reset - empty all state reverting to default values for the current page
	 * 
	 */

	//

}
